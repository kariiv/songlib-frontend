export default class Artist {

    constructor({id, name, created_at}) {
        this.id = id;
        this.name = name;
        this.createdAt = created_at;

        this.songs = [];
    }

    addSong() {

    }
    
    getSongs() {

        return 
    }


    
    userFollow(user) {
        
    }
    
    userUnfollow(user) {
    
    }
}